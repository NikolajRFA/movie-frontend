import {Spinner} from "react-bootstrap";
import React from "react";

const LoadingSpinner = () => <Spinner animation="border"
                                      style={{color: "#FFE920"}}/>

export default LoadingSpinner;